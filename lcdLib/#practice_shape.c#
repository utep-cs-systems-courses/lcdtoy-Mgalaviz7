#include <libTimer.h>
#include "lcdutils.h"
#include "lcddraw.h"

int main(){


  configureClocks();
  lcd_init();
  u_char width = screenWidth, height = screenHeight;

  clearScreen(COLOR_BLACK);
  int x,y;
  int x_origin = 10 ,  y_origin =10;
  /*
  for(y=0; y<=10; y++){
    for(x=0; x<=20; x++){
      drawPixel(x+x_origin+20,y+y_origin+20,COLOR_WHITE);
    }
  }

  for(y=0; y<=10; y++){
    for(x=0; x<=y; x++){
      drawPixel(x+x_origin+40,y+y_origin+20,COLOR_WHITE);
    }
  }
  
  for(y=0; y<=10; y++){
    for(x=0; x<=y; x++){
      drawPixel(-x+x_origin+20,y+y_origin+20,COLOR_WHITE);
    }
  }
  */

  // longer rectangle
  for(y=0; y<=50; y++){
    for(x=0; x<y; x++){
      drawPixel( x+x_origin+50, y+y_origin+50, COLOR_WHITE);
      // drawPixel( -x-x_origin+100, -y-y_origin+100, COLOR_WHITE);
      drawPixel( -x-x_origin+60, -y-y_origin+60, COLOR_WHITE);
    }
  }

  for(y=0; y<=15; y++){
    f
      drawPixel( x+x_origin+50, y+y_origin+50, COLOR_WHITE);
      // drawPixel( -x-x_origin+100, -y-y_origin+100, COLOR_WHITE);
      drawPixel( -x-x_origin+60, -y-y_origin+60, COLOR_WHITE);
    }
  }

  

  return 0;
}
